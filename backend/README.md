# kasa360-api

Railway’de çalışan HTTP API: e-posta OTP, JWT, users/workspaces.

```bash
cp .env.example .env
npm install
npm run migrate
npm run dev
```

Railway: Root Directory = `backend`, Postgres bağla, env’leri doldur.
Health: `GET /health`
